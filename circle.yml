


dependencies:
  override:
    - echo "deps"
    - env| grep -e CIRCLE -e CI
    - set| grep -e CIRCLE -e CI


test:
  override:
    - echo "test"
    - env | grep -e CIRCLE -e CI
    - set| grep -e CIRCLE -e CI



deployment:
  prod:
    branch: /.*/
    commands:
      - echo "deploy to prod!"
      - echo "deploy"
      - env| grep -e CIRCLE -e CI
      - set| grep -e CIRCLE -e CI
